services:
  dev:
    build: .
    image: ht1-dev:latest
    container_name: ht1-dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: /bin/bash
